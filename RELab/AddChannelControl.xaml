<Window x:Class="RELab.DialogWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RELab"
             mc:Ignorable="d"
			 Height="185" Width="350"
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid >
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Label Name="DetailTextBlock" Grid.ColumnSpan="2" FontSize="20" Margin="5,10,0,0">Question:</Label>
		<TextBox Name="InputBox" Grid.ColumnSpan="2" Grid.Row="1" Margin="10" FontSize="20">Answer</TextBox>

		<Button BorderThickness="0" IsDefault="True" FontSize="20" Click="OkButtonClick" Grid.Row="2" Margin="0,10,0,0" MinWidth="80" HorizontalAlignment="Center">Ok</Button>
		<Button BorderThickness="0" IsCancel="True" FontSize="20" Grid.Row="2" Grid.Column="1" Margin="0,10,0,0" MinWidth="80" HorizontalAlignment="Center">Cancel</Button>
	</Grid>

</Window>
